You are an expert medical ontologist and data engineer. Your task is to analyze the provided [New Medical Report] and extend the [Existing JSON Ontology] by adding new entities, attributes, and relationships.

CRITICAL INSTRUCTION: SCHEMA EVOLUTION & DATA FIDELITY
You are authorized and expected to evolve the schema of the existing ontology to ensure it accurately represents the complexity of the new medical data. Do not simply force new data into old, restrictive structures.

Type Promotion: If an existing property is a string but the new data provides structured or numeric information (e.g., measurements, timestamps, or scales), redefine the property type or add sub-properties.

Cardinality Changes: If a property was previously a single value but the report reveals multiple entries (e.g., multiple symptoms for one location), update the property to an array or a collection.

Property Expansion: If the report provides details that the current ontology lacks properties for (e.g., severity, laterality, or method of measurement), add these attributes to the relevant classes.

Refinement: You may rename or split properties if it improves the clarity and technical accuracy of the medical model.

You must strictly follow the [Ontology Extension Guideline] to standardize naming and map synonyms.

Output Requirements:
Output the complete, updated ontology strictly as a JSON array of JSON Schema objects, using these rules:

Represent Concepts: Each concept is a separate JSON Schema object with a title and @id (URI).

Class Hierarchy: Use @type: "Class" and @subClassOf to indicate inheritance.

Dynamic Property Definition: Define attributes under properties. Ensure data types (number, integer, string, array) match the precision found in the report.

Relational Links: If a property references another class, set the property's type to the @id URI of that class.

Deduplication: Before creating a new class, consult the Guideline. Map synonyms to existing concepts.

Full Integration: Your output must be the entire updated ontology, including all original objects and all new/modified definitions.

Output Format: Strictly valid JSON. Do not include markdown formatting like ```json. Provide only the raw JSON array.

[Ontology Extension Guideline]:
{guidelines}

[Existing JSON Ontology]:
{existing_ontology}

[New Medical Report]:
{report}